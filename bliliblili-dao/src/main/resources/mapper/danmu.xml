<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bliliblili.dao.DanmuDao">
    <insert id="addDanmu" parameterType="com.bliliblili.domain.entity.Danmu">
        insert into
            t_danmu(user_id,video_id,content,danmu_time,create_time)
        values
            (#{userId},#{videoId},#{content},#{danmuTime},#{createTime})
    </insert>

    <select id="getDanmus" parameterType="java.util.Map" resultType="com.bliliblili.domain.entity.Danmu">
        select
        *
        from
        t_danmu
        where
        video_id = #{videoId}
        <if test="startTime != null and startTime != '' ">
            and create_time <![CDATA[>=]]> #{startTime}
        </if>
        <if test="endTime != null and endTime != '' ">
            and create_time <![CDATA[<=]]> #{endTime}
        </if>
    </select>

</mapper>
